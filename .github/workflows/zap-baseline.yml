name: ZAP Baseline

on:
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "0 5 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: zap-${{ github.ref }}
  cancel-in-progress: true

jobs:
  zap-baseline:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Start app (e2e profile)
        run: |
          mvn -q -DskipTests spring-boot:run -Dspring-boot.run.profiles=e2e -Dspring-boot.run.arguments=--server.port=8085 &
          echo $! > app.pid
          for i in {1..60}; do
            if curl -fsS http://localhost:8085/actuator/health > /dev/null; then
              echo "App is up"
              break
            fi
            sleep 2
          done
          curl -fsS http://localhost:8085/actuator/health > /dev/null

      - name: Prepare ZAP workspace
        run: |
          # ZAP docker runs as a non-owner user; mount a writable folder for its files.
          mkdir -p zap-work
          chmod 777 zap-work

      - name: ZAP baseline scan
        run: |
          docker pull ghcr.io/zaproxy/zaproxy:stable -q
          set +e
          docker run --rm -v "$PWD/zap-work:/zap/wrk/:rw" --network=host ghcr.io/zaproxy/zaproxy:stable zap-baseline.py \
            -t http://localhost:8085 \
            -J report_json.json \
            -w report_md.md \
            -r report_html.html \
            -a
          code=$?
          set -e
          if [ "$code" -eq 2 ]; then
            echo "::warning title=ZAP baseline::Warnings found (exit code 2). See the zap-report artifact."
            exit 0
          fi
          if [ "$code" -ne 0 ]; then
            exit "$code"
          fi

      - name: Stop app
        if: always()
        run: |
          if [ -f app.pid ]; then
            kill $(cat app.pid) || true
          fi

      - name: Upload ZAP report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: |
            zap-work/report_html.html
            zap-work/report_md.md
            zap-work/report_json.json
          if-no-files-found: warn
