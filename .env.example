DB_HOST=localhost
DB_PORT=5432
DB_NAME=finguard
DB_USER=finguard
DB_PASSWORD=change-me

# Base64-encoded 32+ byte secret, e.g. `python -c "import secrets,base64;print(base64.b64encode(secrets.token_bytes(48)).decode())"`
JWT_SECRET=change-me
JWT_COOKIE_SECURE=true
JWT_COOKIE_SAMESITE=Lax
JWT_REQUIRE_ENV_SECRET=true

MAIL_ENABLED=false
MAIL_FROM=no-reply@finguard.local

APP_SECURITY_CSRF_ENABLED=true
APP_SECURITY_TRUST_PROXY_HEADERS=false
ALLOWED_ORIGINS=https://finwize.tech,http://localhost:8080,http://localhost:5173
APP_SECURITY_AUTH_REQUIRE_EMAIL_VERIFIED=true
