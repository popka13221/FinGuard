# Copy to .env (gitignored) and adjust secrets for local/dev/prod
DB_HOST=localhost
DB_PORT=5432
DB_NAME=finguard
DB_USER=finguard
DB_PASSWORD=change-me

JWT_SECRET=REPLACE_WITH_BASE64_256BIT_SECRET
JWT_COOKIE_SECURE=false
JWT_COOKIE_SAMESITE=Lax
JWT_REQUIRE_ENV_SECRET=false

MAIL_ENABLED=false
MAIL_FROM=no-reply@finguard.local

ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080
